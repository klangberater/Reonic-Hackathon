# Lumen — the home-energy assistant Enpal should ship

**Reonic / Enpal hackathon entry.** Lumen turns a solar home's raw telemetry into one calm
answer to the only question that matters: *"what should I do right now to use more of my own
sun and pay less?"* — and then does it for you.

It is a native iOS app on top of a deterministic energy-planning backend, with a grounded
LLM assistant layered on top. The numbers come from real physics and real data; the words
come from the model. Nothing is faked for the demo: the planner, the savings, the anomaly
detection and the schedule are all computed from a coherent year of 15-minute telemetry.

> **Live API:** `https://getfletcher.ai/api` · **iOS:** SwiftUI (TestFlight) · **Backend:** TypeScript/Express

---

## Table of contents

- [What it does](#what-it-does)
- [Architecture at a glance](#architecture-at-a-glance)
- [Tech stack](#tech-stack)
- [Repository layout](#repository-layout)
- [Quick start](#quick-start)
  - [1. Backend](#1-backend)
  - [2. Dataset](#2-dataset)
  - [3. iOS app](#3-ios-app)
- [Configuration](#configuration)
- [Testing](#testing)
- [Deployment](#deployment)
- [Further documentation](#further-documentation)

---

## What it does

The app is **one conversation with your home**: say (or tap) what you want to get done today,
and it schedules those jobs onto your own solar — then explains the why and reminds you.

### 1. Plan my day (the main screen)
The hero surface. Pick energy-heavy tasks — charge the car, run the dishwasher / washing
machine / dryer, a hot-water or heating boost — give each a "done by" time, and get a
**schedule that runs them on as much solar as possible**. A timeline draws the day's solar
curve with task blocks slotted under it, split-shaded by own-solar vs grid. Toggle
**Cheapest / Greenest / Soonest**, nudge a block, or re-plan; a summary shows
"% solar · € saved · kg CO₂". You can drive the whole thing **by voice** — hold the mic and
say *"charge the car by 7am and run the dishwasher this afternoon"*: speech → text
(ElevenLabs STT) → tasks (GPT-4o) → plan → a spoken summary back (ElevenLabs TTS).

### 2. Ask anything (grounded chat)
A natural-language assistant that **only speaks numbers it computed**. It runs an OpenAI
function-calling loop over the same planner tools the app uses, so "when should I run the
dishwasher?" or "why is my bill so high?" are answered from live data — with the "why" backed
by real **weather-normalised anomaly detection**: a winter **heat-pump fault** or a summer
**solar-soiling** drop, detected from the telemetry, not guessed.

### 3. The glance (Home)
A calm one-screen status: a plain-language verdict ("Running on free solar — sending 7.7 kW
to the grid"), a live power-flow detail, a month-to-date money line ("on track to **earn** €15
this month"), and the flexible-device list — tap a device for an **interactive day-timeline**
to pick when you're home, with a reminder for that slot. A **"Needs a look"** card appears only
when something is genuinely wrong. (Present in the codebase; the current build leads with
Plan-my-day.)

A **Settings** screen switches the demo clock (live · sunny-summer · winter) and light/dark
appearance, and shows the contract.

---

## Architecture at a glance

```
┌──────────────────────────┐         HTTPS / JSON          ┌───────────────────────────────────┐
│        iOS app            │  ───────────────────────────▶ │   Backend (Node + TypeScript)      │
│        (SwiftUI)          │   /now /money /devices         │                                    │
│                           │   /optimize_load /plan_day     │  REST API (Express)  ── server.ts  │
│  RootPager (swipe)        │   /insights /chat ...          │       │                            │
│   ├─ HomeView  (glance)   │ ◀───────────────────────────  │       ├─ planner     optimizeLoad  │
│   ├─ PlanDayView (plan)   │        JSON responses          │       ├─ day planner planDay       │
│   └─ ChatView  (assistant)│                                │       ├─ anomaly     anomaly.ts    │
│                           │                                │       ├─ money       money.ts      │
│  ClockStore (shared now)  │                                │       ├─ ledger      ledger.ts     │
└──────────────────────────┘                                │       └─ chat        openaiChat.ts │
                                                             │              │  (OpenAI gpt-4o)    │
                                                             │       data.ts │ read models views  │
                                                             └───────────────┼────────────────────┘
                                                                             ▼
                                              data/  — 4 homes × 35,040 × 15-min records (2026)
                                              generated by scripts/transform_dataset.py
                                              from enpal-track-dataset/ + real Munich forecast
```

- **No database.** The "source of truth" is a coherent, physically-balanced JSON dataset; the
  only mutable state is an in-memory committed-loads ledger.
- **Tools own the numbers, the LLM owns the words.** Every figure the assistant says is the
  return value of a deterministic function call.
- **Voice in, voice out.** Hold-to-talk → `POST /transcribe` (ElevenLabs STT) → `POST /plan_text`
  (GPT-4o parses the sentence into real tasks → planner → ElevenLabs TTS spoken summary).
- **Deploy is one push.** Push to `main` → GitHub Actions → SSH → build → restart systemd →
  smoke-test. iOS ships via TestFlight.

---

## Tech stack

| Layer | Technology | Notes |
|------|-----------|-------|
| iOS app | **Swift 5 · SwiftUI · iOS 17** | No third-party Swift packages. `URLSession` async networking, `UserNotifications` for reminders. |
| iOS project | **XcodeGen** | `ios/project.yml` is the source of truth; the `.xcodeproj` is generated (gitignored). |
| Backend runtime | **Node.js 20+ · TypeScript 5.5** | CommonJS, compiled with `tsc`. |
| Web framework | **Express 4** | Thin REST layer; endpoints map 1:1 to product surfaces. |
| Tests | **`node:test`** (built-in) | Zero test dependencies; run with `npm test`. |
| Assistant | **OpenAI Chat Completions API** (`gpt-4o`) | Function-calling loop over the planner tools; also parses voice/text into tasks. |
| Voice | **ElevenLabs** (Scribe STT · Turbo v2.5 TTS) | Hold-to-talk planning: speech in, spoken summary out. iOS `AVAudioRecorder`/`AVAudioPlayer`. |
| Data pipeline | **Python 3** | `scripts/transform_dataset.py`; pulls a real **Open-Meteo** Munich forecast. |
| Hosting | **Ubuntu · systemd · nginx + Certbot (TLS)** | `getfletcher.ai`, backend bound to `127.0.0.1:8090`. |
| CI/CD | **GitHub Actions** | `.github/workflows/deploy.yml` (backend), `deploy/testflight.sh` (iOS). |

Full inventory and rationale: **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)**.

---

## Repository layout

```
.
├── README.md                  ← you are here
├── backend/                   TypeScript + Express API
│   ├── src/
│   │   ├── server.ts          REST routes (the public API surface)
│   │   ├── optimizeLoad.ts    single-load planner (source-first, 3 objectives, staggering)
│   │   ├── planDay.ts         multi-task day planner (curve + savings)
│   │   ├── anomaly.ts         weather-normalised heat-pump + solar-soiling anomaly detection
│   │   ├── openaiChat.ts      grounded assistant (OpenAI tool-loop)
│   │   ├── parseTasks.ts      sentence → structured tasks (GPT-4o, strict schema)
│   │   ├── elevenlabs.ts      voice: speech-to-text (Scribe) + text-to-speech (Turbo)
│   │   ├── money.ts           month-end bill / earnings forecast
│   │   ├── devices.ts         flexible-device library + EV-from-charge-target
│   │   ├── contract.ts        tariff / term / notice-deadline logic
│   │   ├── ledger.ts          in-memory committed-loads ledger
│   │   ├── views.ts           read models (snapshot, insights)
│   │   ├── data.ts            dataset access + indexing
│   │   ├── clock.ts           "now" resolution (live · sunny · winter demo clocks)
│   │   └── *.test.ts          node:test suites
│   └── package.json
├── ios/                       SwiftUI client  (see ios/README.md)
│   ├── project.yml            XcodeGen spec
│   └── Sources/               RootPager · PlanDayView · HomeView · ChatView · SettingsView
│       │                      DeviceSheetView · FlowDetailView · ClockStore
│       └── VoiceRecorder · AudioPlayer · NotificationManager · APIClient · Models · Theme
├── data/                      generated, demo-ready dataset  (see data/README.md)
├── enpal-track-dataset/       raw organizer dataset (never modified)
├── scripts/transform_dataset.py   raw → data/ pipeline
├── deploy/                    server setup, systemd unit, nginx/Caddy, TestFlight
├── docs/
│   ├── API.md                 full REST reference
│   ├── ARCHITECTURE.md        technical deep-dive (for evaluation)
│   └── superpowers/           design spec + implementation plan for "Plan my day"
└── .github/workflows/deploy.yml   push-to-main auto-deploy
```

---

## Quick start

Prerequisites: **Node 20+**, **Python 3** (only to regenerate data), **Xcode 16 + XcodeGen**
(only for the app). macOS for the iOS app; the backend runs anywhere Node does.

### 1. Backend

```bash
cd backend
npm install
npm run build        # tsc → dist/
npm start            # → reonic-backend on http://127.0.0.1:8090
```

Smoke-test it:

```bash
curl -s localhost:8090/health
curl -s "localhost:8090/now?household=HH-1001&clock=summer" | python3 -m json.tool
```

Dev loop (rebuild + restart on change):

```bash
npm run dev
```

The LLM endpoints (`POST /chat`, `POST /plan_text`) need `OPENAI_API_KEY`, and the voice
endpoints (`POST /transcribe` + the spoken summary) need `ELEVENLABS_API_KEY`. Every other
endpoint — including the full planner and `plan_day` — works with no secrets. See
[Configuration](#configuration).

### 2. Dataset

The committed `data/` is ready to use — you do **not** need to regenerate it. To rebuild it
from the raw organizer data (and re-pull the Munich forecast):

```bash
python3 scripts/transform_dataset.py     # enpal-track-dataset/ → data/
```

Why a transform exists (the raw data's temperature/heat-pump channels are seasonally
inverted) and exactly what it rebuilds: **[data/README.md](data/README.md)**.

### 3. iOS app

```bash
brew install xcodegen           # one-time
cd ios && xcodegen generate     # project.yml → Lumen.xcodeproj
open Lumen.xcodeproj            # ⌘R to run
```

- **Simulator:** runs as-is — the app points at the public HTTPS API, no local backend needed.
- **Local backend instead:** set `Config.baseURL` to `http://127.0.0.1:8090` (add an ATS
  exception for cleartext localhost) and run the backend from step 1.
- **Real device / TestFlight:** see **[deploy/TESTFLIGHT.md](deploy/TESTFLIGHT.md)**.

> The `.xcodeproj` is generated and gitignored — **always run `xcodegen generate` after
> pulling** so new Swift files are in the target.

More: **[ios/README.md](ios/README.md)**.

---

## Configuration

### Backend (environment variables)

| Variable | Default | Purpose |
|----------|---------|---------|
| `PORT` | `8090` | HTTP port (bound to `127.0.0.1`). |
| `OPENAI_API_KEY` | — | Required for `POST /chat` and the task-parsing in `POST /plan_text`. Without it, those return `503`; the rest of the API works. |
| `OPENAI_MODEL` | `gpt-4o` | Model for the assistant + task parsing. |
| `ELEVENLABS_API_KEY` | — | Required for the voice endpoints (`POST /transcribe`, and the spoken summary in `POST /plan_text`). Planning by text still works without it. |
| `ELEVENLABS_STT_MODEL` | `scribe_v1` | Speech-to-text model. |
| `ELEVENLABS_TTS_MODEL` | `eleven_turbo_v2_5` | Text-to-speech model. |
| `ELEVENLABS_VOICE_ID` | `21m00Tcm4TlvDq8ikWAM` (Rachel) | Spoken voice preset. |
| `DEMO_CLOCK` | `summer` | Default clock when a request omits `?clock=`. |
| `CHAT_TOKEN` | — | Optional shared secret; if set, `/chat` (and the voice endpoints) require header `x-lumen-token`. |

### Request parameters (all endpoints)

- `?household=` — one of `HH-1001`…`HH-1004` (default `HH-1001`, the "hero" home).
- `?clock=` — `summer` (live real time) · `summerday` (fixed Jun 20 midday, solar-soiling demo;
  "Sunny demo" in the app) · `winter` (fixed Jan 15 morning, heat-pump-anomaly demo).
- `?at=` — ISO timestamp override for scripted demos/tests (bypasses the clock).

### iOS

`ios/Sources/Config.swift` — `baseURL` (default `https://getfletcher.ai/api`),
`defaultHousehold`, optional `chatToken`.

---

## Testing

Deterministic backend logic (planner objectives, device library, day-plan orchestration,
savings, staggering) is covered by `node:test`:

```bash
cd backend && npm test     # tsc + node --test  → 12 tests
```

There are no network or model calls in the tests — they run against the committed dataset and
assert real behaviour (e.g. "greenest own-share ≥ cheapest", "soonest start ≤ cheapest",
"tasks stagger across the solar plateau", "savings are positive for an overnight EV charge").

---

## Deployment

**Backend — fully automated.** Push to `main` triggers `.github/workflows/deploy.yml`, which
SSHes to `getfletcher.ai`, fast-forwards the repo, runs `npm ci && npm run build`, restarts the
`reonic-backend.service` systemd unit, and smoke-tests `https://getfletcher.ai/api/health`.
One-time server provisioning: **[deploy/SERVER_SETUP.md](deploy/SERVER_SETUP.md)**.

**iOS — TestFlight.** Archive + upload via Xcode or the scripted path:
**[deploy/TESTFLIGHT.md](deploy/TESTFLIGHT.md)**.

---

## Further documentation

| Document | What's in it |
|----------|--------------|
| **[docs/API.md](docs/API.md)** | Every REST endpoint: parameters, request/response shapes, examples. |
| **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** | Technical deep-dive for evaluation: data pipeline, the planner algorithm, anomaly detection, the grounded chat loop, the real-time clock, iOS architecture, and the key design decisions (what's real vs simulated). |
| **[data/README.md](data/README.md)** | The dataset and the transform that makes it physically coherent. |
| **[ios/README.md](ios/README.md)** | iOS build, structure, and signing. |
| **[deploy/](deploy/)** | Server setup, systemd unit, nginx/Caddy snippets, TestFlight. |
| **[energy-assistant-brief.md](energy-assistant-brief.md)** | The product brief / positioning. |
| **[homescreen-and-backend.md](homescreen-and-backend.md)** | Home-screen anatomy + original backend contract. |
| **[docs/superpowers/](docs/superpowers/)** | Design spec + implementation plan for the "Plan my day" feature. |
