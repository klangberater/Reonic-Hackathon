{
  "name": "reonic-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Energy-assistant backend: REST/SSE for the iOS app + (later) Claude tool-loop over the MCP server",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsc -w -p tsconfig.json & node --watch dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
